import "./App.css";
import Button from "./components/Button";

function App() {
  return (
    <body className="page">
      <Button />
      {/* <Timer
        count={count}
        setCount={setCount}
        showBosalmak={showBosalmak}
        setShowBosalmak={setShowBosalmak}
      /> */}
    </body>
  );
}

export default App;
